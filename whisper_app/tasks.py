from src.whisper import transcribe_audio

def transcribe_task(file_path):
    """
    This function just calls your existing transcribe_audio(),
    but it could do more if you need extra steps.
    """
    return transcribe_audio(file_path)
